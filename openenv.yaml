name: email_env
description: Email triage environment for AI agents
version: 1.0.0

entrypoint:
  module: email_env
  class: EmailEnv

api:
  reset: /reset
  step: /step
  state: /state

observation:
  type: object

action:
  type: object

reward:
  type: number

tasks:
  - name: easy
  - name: medium
  - name: hard
[project]
name = "email-env"
version = "0.1.0"
description = "Email triage OpenEnv environment"
authors = [{ name = "Sachin Singh" }]
dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic",
    "openai",
    "openenv-core>=0.2.0"
]

[project.scripts]
server = "server:app"